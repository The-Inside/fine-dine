spring:
  application:
    name: apigateway

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health , metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    metrics:
      access: read_only